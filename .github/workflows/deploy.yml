name: Deploy to EC2

on:
  push:
    branches:
      - main # Adjust this as necessary for your workflow

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.DEPLOY_KEY }}

      - name: Copy files to EC2
        run: |
          rsync -avz -e "ssh -o StrictHostKeyChecking=no" ./ ec2-user@ec2-3-111-34-64.ap-south-1.compute.amazonaws.com:/home/ec2-user/social
          # You can also use scp if preferred:
          # scp -r -o StrictHostKeyChecking=no ./ ec2-user@ec2-3-111-34-64.ap-south-1.compute.amazonaws.com:/home/ec2-user/social
